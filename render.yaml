services:
  - type: web
    name: gold-mc-anthonio
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn gold_mc_anthonio:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
